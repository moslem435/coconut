import React from 'react'
import { TerminalCore } from '@/os/components/TerminalCore'

const Terminal: React.FC = () => {
  return (
    <TerminalCore className="h-full w-full pt-12" />
  )
}

export default Terminal
