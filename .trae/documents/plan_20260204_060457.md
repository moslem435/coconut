I will fix the issue where the scroll sound effect is inaudible by increasing the volume and enhancing the sound design to make it more distinct.

**Analysis:**
The current implementation of the scroll sound (`playHover`) in `src/lib/sound.ts` is mathematically too quiet.
- Master Volume: 0.1
- Hover Sound Gain: 0.02
- **Effective Volume:** 0.002 (-54dB), which is virtually silent on most speakers.

**Plan:**
1.  **Modify `src/lib/sound.ts`**:
    - Increase **Master Volume** from `0.1` to `0.3` to provide more headroom.
    - **Redesign `playHover()`**:
        - Increase gain from `0.02` to `0.15`.
        - Add a secondary high-frequency "tick" (transient) to the sound. The current low-frequency (200Hz) triangle wave is too muddy; adding a crisp high-pitch decay will make it sound like a proper mechanical UI interaction.

This change will ensure the sound is clearly audible when scrolling.